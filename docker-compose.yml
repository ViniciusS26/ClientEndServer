version: '3.9'

services:
  servertcp:
    build: 
      context: ./src
      dockerfile: projeto_TCP/Dockerfile
    container_name: server_tcp
    volumes:
      - ./src:/app
    networks:
      redes:
        ipv4_address: 95.58.0.3
    ports:
      - "8000:80"


  client:
    build:
      context: ./src
      dockerfile: client/Dockerfile
    container_name: client
    depends_on:
      - servertcp
    volumes:
      - ./capturas:/capturas
      - ./src:/app
    networks:
      redes:
        ipv4_address: 95.58.0.4

networks:
  redes:
    driver: bridge
    ipam:
      config:
        - subnet: 95.58.0.0/24